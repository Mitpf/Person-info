const PersonalCard = ({ name, age, job, car, hobbie }) => {
  return (
    <ul>
      <li>name: {name}</li>

      <li>age: {age}</li>

      <li>job: {job}</li>

      <li>car: {car}</li>

      <li>name: {hobbie}</li>
    </ul>
  );
};

export default PersonalCard;
